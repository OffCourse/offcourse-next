{
  "private": true,
  "devDependencies": {
    "cz-conventional-changelog": "^2.1.0",
    "lerna": "^3.0.0-beta.21"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "echo TEST",
    "build": "lerna run --scope @offcourse/* build",
    "publish": "lerna publish --conventional-commits --canary --yes"
  },
  "version": "0.0.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/OffCourse/offcourse-next.git"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
