query course($courseQuery: CourseQuery!) {
  course(courseQuery: $courseQuery) {
    goal
    avatarUrl
    curator
    fork {
      curator
      goal
    }
    timestamp
    courseId
    description
    revision
    tags
    checkpoints {
      task
      checkpointId
      resourceUrl
      completed
    }
  }
}
