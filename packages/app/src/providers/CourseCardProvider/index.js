import CourseCardProvider from "./Provider";
import { defaults, queries, mutations, resolvers } from "./graphql";

export default CourseCardProvider;

export { defaults, queries, mutations, resolvers };
