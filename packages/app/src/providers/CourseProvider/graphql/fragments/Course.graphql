fragment CourseData on Course {
  goal
  curator
  fork @include(if: $isAuthenticated) {
    curator
    goal
  }
  timestamp
  courseId
  description
  revision
  tags
  checkpoints {
    task
    checkpointId
    resourceUrl
    completed @include(if: $isAuthenticated)
  }
}
