fragment CourseData on Course {
  goal
  avatarUrl
  curator
  timestamp
  courseId
  description
  revision
  tags
  checkpoints {
    task
    checkpointId
    resourceUrl
    resource {
      title
      description
      tags
    }
  }
}
