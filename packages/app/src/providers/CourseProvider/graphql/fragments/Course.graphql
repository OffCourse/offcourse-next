fragment CourseData on Course {
  goal
  fork {
    goal
    curator
  }
  curator
  timestamp
  courseId
  description
  revision
  tags
  checkpoints {
    task
    tags
    checkpointId
    resourceUrl
    completed
    resource {
      resourceType
      title
      description
      tags
    }
  }
}
